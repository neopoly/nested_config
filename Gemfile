source "http://rubygems.org"

# Specify your gem's dependencies in neopoly_config.gemspec
gemspec

group :test do
  gem 'simplecov',        :require => false
  gem 'simplecov-rcov',   :require => false
end
